version: 2

updates:
  - package-ecosystem: "github-actions"
    directories:
      - /
      - /composite-actions/*
      - /reusable-workflows
      - /templates
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"

